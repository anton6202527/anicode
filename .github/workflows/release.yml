name: Release

on:
  push:
    branches: [main]
  release:
    types: [published]

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # 合并到 main 时：有待发布的 changeset 就开「Version Packages」PR；
  # 该 PR 合并后再次触发，此时直接 publish 到 npm。
  npm:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          registry-url: "https://registry.npmjs.org"

      - run: npm ci

      - name: Create Release PR or publish
        uses: changesets/action@v1
        with:
          version: npm run version
          publish: npm run release
        env:
          # GitHub supplies this per job; npm publish uses trusted publishing (OIDC), not a stored token.
          GITHUB_TOKEN: ${{ github.token }}
          NPM_CONFIG_PROVENANCE: "true"

  # 发布 GitHub Release 时：构建 .vsix 并作为 Release 资产上传。
  vsix:
    if: github.event_name == 'release'
    name: vsix (${{ matrix.target }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest, target: linux-x64 }
          - { os: ubuntu-24.04-arm, target: linux-arm64 }
          - { os: macos-latest, target: darwin-arm64 }
          - { os: macos-15-intel, target: darwin-x64 }
          - { os: windows-latest, target: win32-x64 }
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - run: npm ci

      - name: Package VSCode extension
        run: >-
          npx vsce package
          --no-dependencies
          --target "${{ matrix.target }}"
          -o "packages/vscode/anicode-${{ matrix.target }}.vsix"

      - name: Upload .vsix to release
        uses: softprops/action-gh-release@v2
        with:
          files: packages/vscode/anicode-${{ matrix.target }}.vsix
