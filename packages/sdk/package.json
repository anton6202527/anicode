{
  "name": "@anicode/sdk",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "AniCode server 的类型化客户端 SDK：REST + SSE 事件订阅（零运行时依赖，类型自 @anicode/core type-only 导入）",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "codegen": "node --import tsx scripts/generate.ts",
    "codegen:check": "node --import tsx scripts/generate.ts --check",
    "test": "cross-env ANICODE_LANG=zh ANICODE_CREDENTIAL_BACKEND=memory ANICODE_DISABLE_OS_KEYCHAIN=1 node --import tsx --test 'src/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anicode/core": "*"
  }
}
