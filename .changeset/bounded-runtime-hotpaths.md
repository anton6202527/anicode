---
"anicode": patch
---

Reduce long-session latency and memory with row-level SQLite command/outbox operations, bounded LSP and SSE buffers, linear-time streamed tool arguments and repo-map queries, incremental conversation persistence, and concurrent background MCP readiness.
