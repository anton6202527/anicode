---
"anicode": patch
---

Fix global CLI startup by preserving the built-in `custom` provider, defaulting new sessions to DeepSeek, and adding secure AniCode Cloud login so the shared provider key stays on the Supabase gateway.
