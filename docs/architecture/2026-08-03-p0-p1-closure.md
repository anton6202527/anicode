# Core / TUI / CLI P0/P1 生产化闭环

日期：2026-08-03

## 结论与适用范围

本轮已经闭合代码仓库内可落地的 P0/P1 问题，覆盖 Core、SessionManager、TUI/CLI、
IPC/HTTP daemon、网络代理、会话数据、容器/Kubernetes 执行和发布门禁。这里的“闭合”表示
安全不变量已进入实现并有回归测试；它不等于某个组织的生产发布已经获批。真实 PostgreSQL、
对象存储、CNI、KMS/Vault、签名证书、模型质量基线、灾备演练和 30 天 canary 仍须在目标环境验收。

新的生产入口必须同时启用 Workspace Trust、`workspaceScope`、鉴权传输和受控运行时。Core 为旧嵌入方
保留的兼容缺省不是推荐的生产配置。

## P0/P1 闭环清单

| 优先级 | 风险                                                                                            | 已落地控制                                                                                                                                                                                                                                                 | 生产不变量                                                                                                         |
| ------ | ----------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| P0     | 克隆仓库可借项目 env/config、MCP/LSP、hook 或项目扩展在用户确认前执行代码                       | Workspace Trust 将授权绑定到 canonical path、device/inode 和执行面 SHA-256；项目执行来源以及 Git config/hooks/attributes 变化自动撤销；加载前后复核                                                                                                       | 没有显式可信 assessment 就 fail closed，模型或项目内容不能自行授信                                                 |
| P0     | 同一宿主可加载其他 cwd 的历史或复用一份静态项目配置                                             | 本地 CLI、HTTP serve、Desktop 和 VS Code 的 `SessionManager` 均配置 exact canonical `workspaceScope`；列表过滤域外会话，create/open/resume/send/fork 在读取正文或构造 provider 前拒绝越界                                                                  | 一个 scoped manager 只服务启动时的同一真实工作区；符号链接别名不能扩张边界                                         |
| P0     | HTTP daemon 无凭证、明文暴露或与本机 CLI Proxy 抢占 8317                                        | HTTP daemon 只绑定 loopback，除健康检查外强制 Bearer；`serve` 始终发布私有 token file；默认端口改为 8327                                                                                                                                                   | 8317 保留给内置 CLI Proxy provider；远程访问必须经 HTTPS/mTLS 反向代理                                             |
| P0     | CONNECT 代理成为任意 TCP/SSRF 隧道，或 DNS 检查后再次解析                                       | CONNECT 只接受 DNS authority 和有界 TLS ClientHello；拒绝非 TLS、无 SNI、SNI 不匹配、ECH/ESNI 与 IP authority；验证后直连已批准的 pinned IP                                                                                                                | 代理授权的域名、SNI 和最终连接 IP 必须是同一目标                                                                   |
| P0     | 删除会话时并发 send/recovery/artifact 写入在 purge 后“复活”内容                                 | SQLite/PostgreSQL 原子 lifecycle transition 阻止新 lease，并等待旧 lease；path+device/inode 防目录替换；S3 先写永久删除标记、隐藏并拒绝迟到写，再由可重启 reconciler 反复清前缀                                                                            | transcript/runtime 的删除是强 fence；S3 物理清除是有告警和 SLO 的持久收敛过程，不能谎称瞬时 physical-zero          |
| P0     | 超时、取消、租约丢失或并发写导致残留容器、过期 worker 提交或半应用文件                          | AbortSignal 贯穿 client/worker/container/PatchSet；worker、worktree 和 PatchSet 使用 fencing token；事务写在临时副本生成差异，成功后原子 PatchSet apply，取消/冲突回滚                                                                                     | 失去租约或收到取消的执行不能提交副作用                                                                             |
| P0     | 远端 runner 直接写 PVC，或经未认证网络绕过出口策略                                              | 容器要求 digest-pinned image、内部网络、受控 proxy、只读 rootfs、cap-drop、no-new-privileges 和资源限制；长效 proxy control token 只走独立 HTTPS control URL，runner 仅拿每次执行短期 capability；Kubernetes source/PVC 只读、无 SA token、默认拒绝 egress | Kubernetes `workspace-write` 在可信控制面 PatchSet committer 接线前明确拒绝，不会降级为直接写 PVC                  |
| P0     | 并发配额检查先读后写，多个 worker 同时越过上限                                                  | PostgreSQL enqueue quota 在 `SERIALIZABLE` 事务与 advisory lock 内检查和插入；tenant/actor 限额原子生效                                                                                                                                                    | 同一配额域的竞争提交最多放行允许数量                                                                               |
| P0     | 宿主 Git 操作继承 PATH、仓库 filter/hook/fsmonitor 或外部 gitdir 执行不可信代码                  | 控制面固定可信 Git 可执行文件，禁用 system/global/repository 配置执行面；快照使用独立 config-free control view，并验证 gitdir/commondir/object DB 均在工作区边界内                                                                                           | 自动 checkpoint、worktree、revision 与 GitHub runner 不执行仓库提供的 Git 扩展                                     |
| P1     | Remote job 排队后继续持有过期授权、API 提前宣称取消完成，或 bearer 经明文入口暴露               | job 持久化 actor/tenant/workspace/capability 与硬过期时间，claim/执行前/运行中重验；运行中取消先持久化 `cancellation_requested`，仅 fencing lease owner 停止后 ack；非回环监听必须使用 native TLS 或显式 trusted-proxy 契约                                | 过期或被权威策略拒绝的 grant 不能开始/继续执行；未收到 worker ack 不能声称副作用已停止；public bearer 不走明文链路 |
| P1     | HTTP/SSE 重连、慢客户端和 replay 缓冲造成重复加载或无界内存                                     | replay ring 同时按事件数和字节数有界；慢客户端有 pending bytes/events 上限并主动断开；feed load 去重、初始化完成后才注册 live feed；关闭等待 pending load                                                                                                  | 一个会话/manager 只有一个 authoritative feed，慢客户端不能拖垮服务                                                 |
| P1     | Artifact 下载在返回 iterator 后提前释放 lifecycle lease，导致 DELETE 已完成的旧响应继续泄露字节 | 读取 lease 覆盖 iterator 完整生命周期；删除 fence 主动 abort 并 drain active reader；iterator `finally` 取消上游 S3 body                                                                                                                                   | `deleteSession` 返回后，任何旧 artifact response 都不能继续产生 payload bytes                                      |
| P1     | JSONL 兼容迁移在 scoped host 授权前遍历并导入所有 legacy transcript                             | `MigratingSessionStore.list()` 只合并 metadata；正文按 session 懒迁移；同 ID 不同 workspace 的 legacy/primary 记录不合并                                                                                                                                   | 域外 prompt/response 在 metadata scope 校验前既不读取，也不写入 primary                                            |
| P1     | `/model` 展示静态、过期或由客户端错误网络环境探测出的模型                                       | 模型发现成为 `SessionHost` 能力，Local/IPC/HTTP 都在 authoritative host 进程访问鉴权 `/models`；TUI 有界并发、总超时、代次隔离，并在选择和 `once` 前重验                                                                                                   | 当前宿主未成功返回的模型不进入本次选择器；探测失败不永久删除静态 catalog                                           |
| P1     | Provider/MCP/web 与仓库检索可因恶意流、超大文件或目录树耗尽宿主资源                              | Provider registry 按 runtime stack 隔离 credential/proxy；响应、SSE、文本、tool args、事件数、文件/目录数、单文件与总扫描量均有硬上限和取消；glob/grep 不再启动仓库可影响的 `rg`                                                                          | 每个请求和每个工作区的 CPU/内存/网络消耗都有可审计硬边界，实例之间不串用凭据或出口                                 |
| P1     | LSP 返回域外 URI、repo-map 缓存经 `.anicode` symlink 写宿主，或索引无界增长                      | LSP 输入/返回 canonical containment、普通文件与 8 MiB 上限；repo-map/TypedCodeGraph 使用私有进程缓存并限制文件数、源字节、JSON、向量维度，显式 workspace cache fail closed                                                                                | 语言服务和索引不能越过 workspace，也不能把仓库路径当可信缓存目录                                                   |
| P1     | 多 manager 浏览器全局共享，dispose/launch 竞态导致跨会话关闭或残留进程                           | BrowserRegistry 由 manager 持有，跟踪 pending launch 并在 close 后拒绝注册；构造回滚与 dispose 等待本实例全部浏览器退出                                                                                                                                   | 一个 manager 的生命周期不能影响或泄漏另一个 manager 的浏览器                                                      |
| P1     | 原生 Windows 没有已实现的 OS sandbox，却仍向模型暴露任意命令或由项目配置启动子进程              | Local runtime 明确进入 `restricted` execution mode；正常与 Workspace Trust restricted registry 均移除 shell/LSP/browser 进程面，并禁用项目 hooks、自动 verifier、checkpoint git filter 与 worktree subagent；进程后端始终 fail closed                      | 信任项目或批准权限都不能扩大宿主执行能力；只有 digest-pinned OCI 后端可恢复前台命令执行                            |
| P1     | TUI 结果不可滚动/框选，终端状态异常，日志无限增长                                               | 结果视口、固定 composer、原生文字框选、PageUp/PageDown、alternate-scroll、SIGCONT raw-mode 恢复、Ctrl+Z/Ctrl+Q、plain/no-color、bounded debug/transcript 与 PTY smoke 已接线                                                                               | TUI 行为不能依赖单一终端；调试数据不能无界增长或默认记录内容                                                       |
| P1     | 发布只跑局部测试、执行未审计 lifecycle script，或让仓库代码接触签名/OIDC/上传权限                | `verify:release` 覆盖完整门禁；install script 使用精确版本/registry/integrity/signature allowlist；npm gate+pack、version PR、OIDC tarball publish 分 job；桌面签名最小 step，构建与上传/attestation 分权                                                   | 任一步失败即停止；持权发布 job 不 checkout 或执行仓库代码，所有产物来自同一 gated revision                         |

## Workspace Trust 与 restricted mode

### 操作命令

```bash
# 查看当前真实路径、信任状态、原因和执行来源；只读，可输出 JSON
anicode trust status --cwd "$PWD"
anicode trust status --cwd "$PWD" --json

# 必须在真实交互终端中逐字输入 canonical workspace path
anicode trust grant --cwd "$PWD"

# 立即撤销；下一次 open/send 会重新评估
anicode trust revoke --cwd "$PWD"
```

`trust grant` 不接受 `--json`、管道输入或非 TTY 自动化。确认期间如果工作区 identity 或执行面指纹发生
变化，grant 会拒绝，用户必须重新审查。trust store 的目录/文件权限为 `0700`/`0600`，并使用带
PID+随机 token 的跨进程锁、原子 rename 与 fsync；活进程持有的旧锁不能仅因时间到期被抢占。

未信任或检查失败时，入口仍可打开工作区，但只进入 restricted mode：

- 对 `not-trusted`、`execution-config-changed` 等可验证的未信任状态，交互式普通入口使用 `default`
  逐项授权，而非固定为内部严格只读策略。受控 registry 保留内置 `read`、`glob`、`grep`、`write`、`edit`、
  `apply_patch`、`bash`、`todo_write`，以及 `bash_output`、`write_stdin`、`list_shells`、`kill_shell`
  等 shell 生命周期工具；写入、命令和 shell stdin 必须逐项确认，受限 `bash` 固定使用
  workspace-write sandbox 且没有联网参数；
- `inspection-failed` 无法证明安全边界，即使请求 `default` 也严格退回只读安全边界：只提供
  `read`、`glob`、`grep`，不提供写入或 shell；
- 禁用会话级 PatchSet、持久化授权、permission profile、MCP、LSP、browser、web search、hooks、
  subagent、skills、project memory、repo map、checkpoint、workspace network proxy 与自定义工具；
- 显式 `--auto` / `--accept-edits` 退回严格只读工具面，运行时 mode/profile 切换也不能扩大
  restricted policy；无头 CLI 与无交互 MCP 入口无法完成逐项确认时 fail closed；
- 项目 `.env*` 不加载，项目执行配置与项目插件/agents/skills/commands 不发现；
- TUI 以 `SessionSnapshot.workspaceTrust` 为 authoritative 状态，opening/reconnect 阶段先按未信任处理，
  不沿用上一个会话的 UI 状态；
- trust resolver 抛错、返回无效结构或执行面在 send 前改变，都按未信任处理。

Workspace Trust 不是 shell sandbox；它只决定能否启用经审查的项目能力，本身不制定工具批准策略。
受信任的本地交互 TUI 由宿主明确以最高权限和自动批准作为初始策略，但仍服从显式 `deny` / `ask`、
sandbox、网络、凭据、runtime 与 workspace scope。未信任、远端和无头入口保持保守。restricted mode
中的内置 `apply_patch` 是逐项授权的文件工具，不等同于会话级 PatchSet prepare/apply/recovery 工作流。

## Windows 与非原生隔离宿主

当前受支持的原生 OS 隔离后端只有 macOS Seatbelt 与 Linux namespace/bubblewrap。原生 Windows（以及
其他没有已实现 sandbox 的平台）不会降级为裸 `cmd.exe`、PowerShell 或 shell spawn，而是进入独立于
Workspace Trust 的 `restricted` execution mode：

- 模型 schema 不含 `bash`、`bash_output`、`write_stdin`、`list_shells`、`kill_shell`、LSP 导航/
  diagnostics 或本地 browser；文件读写、检索、受控 HTTP 和 todo 仍可用；
- 项目命令 hooks、自动 package-script verifier、git checkpoint 和 worktree subagent 不装配；
- runtime 是一个具体的拒绝后端而不是 `undefined`，且故意不实现 `prepare()`；因此 stdio MCP、LSP 或
  其他持久 sidecar 不能回退到旧的裸 spawn 路径；HTTP MCP 不受影响；
- 宿主/插件注入工具必须声明 `capabilities`；restricted/container 模式对未声明能力的工具 fail closed，
  并按 `process`/`persistent-process` 能力剔除，而不是依赖工具名称黑名单。插件 JavaScript 一旦被加载本身
  就属于可信宿主代码边界，因此生产插件还必须经过签名、来源和代码审查，不能把 capability 声明当作沙箱；
- Workspace Trust 与权限模式只能进一步收紧，不能重新引入上述工具。

需要命令执行时必须显式选择 OCI 后端，并把镜像固定到 SHA-256 digest（mutable tag 会在启动时拒绝）：

```powershell
$env:ANICODE_EXECUTION_BACKEND = "container"
$env:ANICODE_RUNTIME_IMAGE = "ghcr.io/OWNER/anicode-runner@sha256:<digest>"
npm run dev:tui
```

当前 OCI runtime 是一次一容器的临时执行，因此只暴露前台 `bash`，schema 中不提供
`run_in_background`，也不暴露后台 shell 控制工具。命令 hooks 可作为单次 OCI 任务运行，因为每次都有
不可变容器 ID 与同步删除证明；它们不会在 native/restricted production 中执行。stdio MCP、LSP 与本地
Chrome 需要受控的持久 sidecar backend；在它实现前保持 fail closed，可分别改用 HTTP MCP、远端语言服务
或显式 CI 验证。

## 命令硬取消边界

- macOS/Linux 的每次隔离命令都在独立 process group 中启动。超时或 `AbortSignal` 先向整个组发送
  `SIGTERM`，有界等待后升级为组级 `SIGKILL`，并等待 leader 关闭及 process group 消失后才返回；只杀
  shell 父进程不算取消完成。即使父进程以 0 正常退出，也会检查并回收仍留在该组的后台后代，避免用
  `cmd &` 绕过 lifecycle boundary。
- Windows 如果未来由可信宿主直接调用同一执行边界，使用 `taskkill /T /F` 并等待目标进程树关闭；不会
  降级成只对父进程调用 `child.kill()`。当前 Windows production composition 仍默认 restricted，任意命令
  执行需要 digest-pinned OCI 后端。
- OCI timeout/abort 会先终止本地 Docker/Podman CLI 进程树，再按本次生成的唯一容器标识依次、同步等待
  `stop` 与 `rm --force`。cleanup 不继承已触发的请求 signal，避免“取消 cleanup”后谎称容器已停止；
  创建前先写入有界、原子替换的 ownership journal，并持久记录
  `reserved → creating → identified(containerId)` 阶段。`creating` 的一次 404 不能证明迟到 create 不会
  提交；只有不可变 ID 或已观察到的单一 owned 对象完成删除证明后才清理记录。
- production Verifier 不再直接 `spawn` workspace package scripts；检查必须走同一个 `ExecutionRuntime`，
  固定断网、workspace policy 与脱敏环境。整轮检查共享一个 writable disposable clone，并按 workspace
  writer 串行，避免 typecheck/test/lint 并发改同一构建缓存造成非确定性；
  `TransactionalExecutionRuntime` 通过一等 discard boundary 直接调用隔离 delegate，永不生成/应用 PatchSet，
  所有成功或失败写入在检查结束后删除。检查前后对 Git tracked、非 ignored untracked 与本轮显式 changed
  files 计算 revision digest，真实工作区被外部并发修改就让证据失效。没有 runtime、没有适用/required
  检查、required 检查被依赖失败跳过，均是 verification failure，不能清除 dirty 状态或发送 `done`。
- production composition 在 spawn 前拒绝 stdio MCP；普通 process group 不能约束主动创建新 session 的
  第三方 server，因此只有未来由 cgroup、OCI sidecar 或 Windows Job Object 持有并证明清理时才可重新开放。
  `prepare()` 仅描述如何启动，不是恶意进程树的清理证明；LSP 还必须要求
  `managedProcessBoundary=close-confirmed`，当前生产 runtime 均不声明该能力。命令 hook 只在经过 core
  attestation 的 `TransactionalExecutionRuntime(ContainerIsolatedRuntime)` 中启用。本地 Chrome 使用脱敏
  环境和 tree termination；browser 全局关闭会聚合并上抛收尸失败。
- `git` 环境采集与 checkpoint plumbing 是宿主控制面的短命只读/快照命令，不接收模型拼接的 shell
  字符串；它们使用固定可信可执行文件、config-free control view、脱敏环境、独立 process group 和硬
  deadline。`glob`/`grep` 使用进程内有界遍历，不再启动 PATH 中的 `rg`。生产入口不允许这些探测静默继承
  provider/MCP 凭据或在 deadline 后留下孙进程。
- 任意进程内 JavaScript Tool 没有安全的强制终止原语；`AbortSignal` 对这种工具只是协作式取消。生产中的
  副作用工具必须把工作放进 `ExecutionRuntime`，或放进带 fencing/事务提交的隔离 worker，不能在 host
  进程里执行不可回滚副作用后仅靠 Promise timeout 宣称已取消。

## HTTP daemon 的安全启动

推荐的本机启动与连接方式：

```bash
# 默认监听 127.0.0.1:8327，启动成功后打印 token file 路径，但不打印 token
anicode serve --cwd "$PWD"

# 本机默认端口可自动推导 token file；显式写出更适合服务管理器
anicode --http http://127.0.0.1:8327 \
  --http-token-file /private/runtime/path/anicode.sock.http-8327.token
```

运维约束如下：

- 默认端口是 `8327`；`8317` 是内置 CLI Proxy/OpenAI-compatible provider 的端口，二者不再冲突；
- `serve` 先成功 bind，再原子发布 token file，避免 `EADDRINUSE` 时提前轮换仍在运行的服务凭证；
- token 至少由 32 个随机字节生成，token 目录/文件必须为当前用户拥有且分别是 `0700`/`0600`，
  symlink、超大文件、宽权限或错误 owner 都 fail closed；
- Unix socket 自定义父目录也必须是当前 uid 拥有的真实目录且精确为 `0700`；生产 server 无 token
  不能构造，唯一无鉴权入口是显式 test-only escape hatch；
- 除 `/healthz` 和 `/global/health` 外，REST、SSE 和模型发现都需要 Bearer；比较使用恒定时间函数；
- 客户端优先使用 `--http-token-file` 或 `ANICODE_HTTP_TOKEN_FILE`。兼容用
  `--http-token`/`ANICODE_HTTP_TOKEN` 仍存在，但可能暴露在 shell history 或进程参数中；
- server 只接受 `127.0.0.1`、`localhost` 或 `::1`。远端访问应由同机 HTTPS/mTLS 反向代理终止 TLS，
  同时保留 bearer、限流、请求体/SSE 上限和租户路由策略。

## Workspace scope

`workspaceScope` 是数据租户边界，不是展示层筛选。它在构造 manager 时 canonicalize 一次，并在每次
create、list、cold load、open/resume/send 和 fork 时重验目标 cwd。实现只接受与 scope 完全相同的
canonical workspace；不会因为字符串前缀相同而把 sibling 或子目录当成同一租户。

对域外会话：

- `listSessions` 静默过滤，避免泄漏 title/model/cwd；
- 已知 session ID 的 open/resume/send/fork 也返回统一 scope error；
- cold load 先检查 metadata header，正文读取后再复核，防止 list/load 之间被替换；
- legacy JSONL 迁移的 `list()` 只读取 metadata，只有该 session 通过上述 scope 预检后才读取并导入正文；
  同 ID 但 workspace identity 不同的 legacy 记录不会覆盖 primary；
- provider 构造、PatchSet recovery、runtime event 和 transcript 写入都发生在 scope 校验之后。

需要服务多个项目时，应为每个 canonical workspace 创建独立 manager/进程或由可信路由层创建独立实例，
不能取消 scope 后依赖 TUI 选择器隔离。

## `/model` 的实时发现语义

每次打开 `/model` 都刷新当前宿主可见的 provider 目录。远程 CLI 不在客户端读取本机 env 或访问模型
端点，而是通过已鉴权的 daemon IPC/HTTP 请求由服务端探测，因此结果与真正执行模型的进程一致。

当前约束：

- 每个 OpenAI-compatible provider 使用鉴权 `GET /models`，不发送 completion；debug provider 返回其
  本地目录；当前 adapter 不支持的 embedding、rerank、image、audio、realtime 等专用模型会过滤；
- provider 单次发现默认 2 秒、响应体最多 1 MiB、最多保留 500 个安全 model ID；TUI 默认并发 4，
  整轮最多 8 秒；
- 只有本轮成功响应的 provider/model 会展示；成功但空列表意味着没有可选模型，超时、401、429、5xx
  或网络错误在本轮 fail closed 为隐藏；
- 新一轮探测或会话切换会使旧代次结果失效，慢响应不能覆盖新结果；鼠标/Enter 选择、
  `/model <provider/model>` 和 `once` 在使用前再次查询 authoritative host；
- 探测结果是瞬时视图，不写回并删除内置 catalog。瞬时 429/5xx 只影响本次选择器；
  `--list-models` 仍是离线内置目录，不代表在线可调用。

这种设计比逐模型 completion ping 更合理：不会为打开选择器产生推理费用、消耗 token/配额或触发批量
限流，也能及时发现服务端新增/删除模型。它证明的是“宿主当前列出该模型”，不是对下一次推理成功的
保证；模型仍可能在选择后因并发配额、区域策略或服务故障失败，正常调用链必须保留错误处理和 fallback。

## 会话删除、保留与备份

显式删除是内容清除操作，`SqliteRuntimeDatabase.prune()` 是时间保留操作，两者不能混用：

- `deleteSession` 通过共享 SQLite/PostgreSQL lifecycle row 原子进入 `deleting`，拒绝新的 producer lease，
  中断并等待 live drive、cold load、command recovery 和已登记持久化操作，然后清除 command inbox、
  outbox、artifacts、runtime event/snapshot 与 transcript；这里的保证来自原子存储 transition，不应表述为
  “第一个 await 前”的进程内布尔值；
- lifecycle 同时持久化 canonical workspace 与 device/inode；目录被替换时 PatchSet 清理 fail closed，不能
  根据复用的字符串路径触碰新目录；`applying` journal 会在同一 workspace/session lock 内先做校验回滚并
  terminalize，真正的哈希冲突才让删除保持可重试；
- 删除后只保留 `session.deleted` 空 tombstone，供同步客户端观察删除，不含 prompt/response；
- 新写入的 File/S3 artifact payload 按 session 隔离。S3 在 session 前缀外先写永久、空内容删除标记；
  marker 立即让读取/再写 fail closed，版本化与非版本化前缀均按最多 1000 项批量清理；
- S3 可能在 producer 崩溃后才确认旧 Put，因此物理清除采用 retained marker + 周期/重启 reconciler 的
  持久收敛，而不是不可能证明的瞬时零残留。生产默认 60 秒，并必须有独立 maintenance run、失败告警、
  erasure SLO 与 Object Lock/MFA-delete 演练；
- 旧版全局 content-addressed blob 可能被多个 session 引用，因此只保留只读兼容，单会话删除不会移除。
  运维方应做引用审计/离线迁移后再按对象存储生命周期回收，不能由一个会话直接删共享 blob；
- `prune()` 默认只清理过期 audit、artifact 索引、terminal/job/event/outbox 事实，不自动删除用户会话或消息。
  用户内容必须经显式删除或组织定义、验证过的独立生命周期流程处理。

备份 SQLite 时必须使用 online backup 或同时一致快照 DB、`-wal` 和 `-shm`。PostgreSQL/S3 需要匹配的
生命周期、跨存储删除检查和定期 restore drill；“数据库行已删”不能当作对象 payload 已删的证据。

## 容器、Kubernetes 与写入事务

### Container backend

- image 默认必须使用 `@sha256:` 固定；root filesystem 只读，drop ALL capabilities，禁止提权，限制
  PID/CPU/memory，`/tmp` 使用 noexec/nosuid/nodev tmpfs，并以调用用户 UID/GID 运行；
- 当前所有任务都使用 `--network none`；共享 internal network 加 proxy 环境变量不能证明 proxy-only
  egress，因此联网容器在专属网络命名空间实现前 fail closed。该限制不修改宿主 DNS、路由、防火墙或
  全局代理；凭据仅通过 Credential Broker 短租约注入并在输出中脱敏；
- `.git` 与 `.anicode` 即使在 workspace-write 也额外只读；timeout、AbortSignal、Docker CLI 错误等所有
  退出路径都等待 `rm --force`。控制器先 `create` 惰性对象，校验 immutable ID/owner label 并再次检查
  取消，才会 `start`；创建结果不明时绝不启动，journal 保留 creating 状态而不是用时间窗冒充清理证明；
- 生产 remote/local stack 用 `TransactionalExecutionRuntime` 包裹 container backend：写命令在临时副本
  运行，失败/超时丢弃；成功时检查 source 未并发改变，再通过带锁与 fencing 的 PatchSet 原子提交。

### Kubernetes backend

- runner 使用 digest-pinned image、non-root、RuntimeDefault seccomp、read-only rootfs、drop ALL、
  `automountServiceAccountToken: false`、resources/active deadline/TTL；
- Job 初始固定为 `suspend: true`，只有同时测试返回 UID 和 owner token 的 JSON Patch 才能激活；模糊的
  create POST 只按确定性名称/UID 观察和清理，绝不在 finally 重发 POST。shutdown 先 fence/abort/drain，
  等 UID 及其 Pods 消失后才关闭 API transport；
- PVC/source mount 始终只读；ephemeral 模式只把 source 复制到受大小限制的 `emptyDir`，不会把 runner 的
  临时写入自动同步回 PVC；
- NetworkPolicy 默认拒绝，联网 Job 只允许到认证出口 proxy；上线前必须在目标 CNI 运行
  `deploy/remote-runtime/verify-isolation.sh` 覆盖 IPv4/IPv6、DNS、metadata、私网和无鉴权代理绕过；
- 当前实现对 Kubernetes `workspace-write` 明确 fail closed。只有在可信 control-plane PatchSet committer
  能验证 base hash、审批、fencing token 并原子提交时，才可重新开放；不得改成 runner 直接挂载可写 PVC。

## 发布门禁与验证证据

运行时支持 Node `>=22.15.0`；正式发布工作流固定 Node 24 LTS，并要求 npm
`>=11.5.1 <12` 以保持供应链可复现。CI 另外在最低 Node 版本和 `current` 最新稳定版执行完整质量门禁：

```bash
npm ci --ignore-scripts
npm run rebuild:trusted
npm run verify:release
```

门禁按顺序执行：

1. `format:check`、`lint --max-warnings 0`、全 workspace `typecheck`；
2. SDK/OpenAPI `codegen:check` 与全量测试（含 Workspace Trust、scope、daemon、SSE、model discovery、
   deletion fence、proxy、container/Kubernetes、PatchSet/worker；PostgreSQL integration 在配置测试数据库时
   必须运行，正式 CI/release 不得以缺少 URL 为由 skip）；
3. production dependency high audit、build dependency policy audit、registry signature audit，以及精确
   `package@version` lifecycle-script allowlist/integrity/registry 审计；
4. clean-room CLI package build/PTY smoke、Electron App build、VS Code extension build和 bundle size budget。

发布批准还必须附带目标环境证据：Linux/macOS/Windows required CI、真实模型 eval 基线与非回归、macOS
签名/公证、Windows 签名、checksum + GitHub attestation 验证、PostgreSQL failover/restore、S3 lifecycle、
Vault/KMS/OIDC 轮换、CNI isolation、终端兼容矩阵、回滚演练和至少 30 天 measured canary。任一项缺失时，
准确表述是“仓库内 P0/P1 控制已实现，生产发布批准待外部证据”，不能写成“production approved”。

## 回归时必须守住的边界

后续新增项目级自动加载目录、可执行配置字段、daemon route、模型协议、artifact backend 或 remote write
path 时，至少同步完成以下工作：

1. 将执行来源加入 Workspace Trust fingerprint 与 restricted filter；
2. 在入口配置 `workspaceScope`，并测试 symlink、目录替换和跨 cwd session ID；
3. 为新 route 增加认证、schema、body/response/SSE 上限、deadline 和取消；
4. 让删除 fence 覆盖新的 session-scoped producer/storage，并验证 purge 后不能重建内容；
5. 让写入经过 transaction + fencing + rollback，远端身份和网络继续 fail closed；
6. 把测试、codegen、审计、build/smoke 纳入同一个 `verify:release`，而不是在发布后补跑。
